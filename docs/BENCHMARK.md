# Benchmark protocol

Use the repository load-test surface to measure p50/p95/p99 latency, throughput, error rate, concurrency, dependency latency and resource saturation.

Record source revision, workload, concurrency, environment and dependency configuration. Recalibrate SLOs from measured workloads. Local benchmark numbers are not production SLO evidence.